{% extends 'base.html.twig' %}

{% block body %}
    <h1>Paysusa</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paysUsa.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ paysUsa.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('adminPaysUsa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('adminPaysUsa_edit', { 'id': paysUsa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
