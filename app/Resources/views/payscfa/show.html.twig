{% extends 'base.html.twig' %}

{% block body %}
    <h1>Payscfa</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paysCfa.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ paysCfa.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('adminPaysCfa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('adminPaysCfa_edit', { 'id': paysCfa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
